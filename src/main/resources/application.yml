server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          default-label: main
          uri: https://github.com/rafaelpossenti/microservice-repo
          clone-on-start: true
